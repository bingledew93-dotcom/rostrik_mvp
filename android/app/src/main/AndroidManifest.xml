<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for AlarmManager.setAlarmClock-quality scheduling on Android 12+.
         Without this, flutter_local_notifications silently downgrades to inexact. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Lets the alarm fire as a full-screen intent that wakes the lockscreen. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Android 13+ runtime permission for displaying notifications at all. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Needed by the plugin's BootReceiver to re-arm pending alarms after reboot.
         Without it, every scheduled alarm is silently dropped on power-cycle. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Required so the alarm can fire while the device is dozing/asleep. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Lets the alarm channel vibrate. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:label="rostrik_mvp"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:showWhenLocked="true"
            android:turnScreenOn="true">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Re-schedules pending alarms after a device reboot. The plugin's
             merged manifest already declares this, but we list it explicitly
             so the manifest is self-documenting and survives plugin upgrades
             that might change the merge behavior. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Receiver the OS dispatches to when a scheduled alarm fires. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />

        <!-- Receiver the OS dispatches to when the user taps a notification
             ACTION BUTTON (Snooze / Dismiss). Required because our actions
             use showsUserInterface:false — Android delivers the tap as a
             silent broadcast Intent rather than as an Activity launch. If
             this receiver is not registered, the broadcast is dropped on
             the floor and our onDidReceiveBackgroundNotificationResponse
             callback never fires. Without it, action button taps look
             completely dead from the Flutter side. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
